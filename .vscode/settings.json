{
    "python.languageServer": "Pylance",
    "python.analysis.typeCheckingMode": "basic",
    "python.analysis.diagnosticSeverityOverrides": {
        "reportMissingModuleSource": "none"
    },
    "python.terminal.activateEnvironment": false,
    "micropico.openOnStart": true,
    // Files MicroPico must not upload. This list REPLACES the extension's default
    // rather than extending it, so the defaults are repeated here -- dropping them
    // would start syncing .git and .vscode to the board.
    //
    // README.md, the schematic PDF, and the .code-workspace are already excluded by
    // micropico.syncFileTypes (md/pdf/code-workspace aren't in it). The two below are
    // not: .html and .py are both sync types, but these run on your computer, not the
    // Pico -- ble_test.py imports bleak, which does not exist in MicroPython.
    //
    // sd_check.py runs on the Pico, but only via "Run current file", which sends the
    // source over the REPL. It is a bench tool, not part of the payload, so it does
    // not belong in the upload.
    //
    // sd_format.py is NOT ignored: the configurator's Format SD card button runs
    // `import sd_format` on the board, so the module has to be uploaded with the
    // project. It costs ~7 KB and nothing imports it at boot.
    "micropico.pyIgnore": [
        "**/.picowgo",
        "**/.micropico",
        "**/.vscode",
        "**/.gitignore",
        "**/.git",
        "**/.DS_Store",
        "**/project.pico-go",
        "**/env",
        "**/venv",
        "**/.venv",
        "**/.idea",
        "configurator.html",
        "ble_test.py",
        "sd_check.py"
    ],
    "python.analysis.typeshedPaths": [
        "~/.micropico-stubs/included",
        "${workspaceFolder}/lib"
    ],
    "python.analysis.extraPaths": [
        "~/.micropico-stubs/included",
        "${workspaceFolder}/lib"
    ]
}